# Rule-Based Chatbot 

## 📌 Objective
Create a simple *rule-based chatbot* using Python and control flow (if-else statements).

## 🛠 Tools Used
- Python 3

## 📂 Description
This project implements a basic chatbot that:
- Accepts user input
- Uses if-else and loops
- Produces responses based on predefined rules

The chatbot continues chatting until the user types *"bye"*.

## ▶ How to Run
1. Install Python (if not installed)
2. Download the script
3. Run:
   ```bash
   python bot.py
