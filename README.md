# python-rule-based-chatbot
This project is a basic rule-based chatbot created using Python. It uses input handling, if-elif-else conditions, and loops to simulate simple conversational responses. The chatbot continues interacting with the user until the exit command is given. 
